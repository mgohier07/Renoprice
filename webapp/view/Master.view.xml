<mvc:View
	controllerName="sap.ui.demo.masterdetail.controller.Master"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m"
	xmlns:semantic="sap.m.semantic">

	<semantic:MasterPage
		id="page"
		title="{masterView>/title}"
		navButtonPress="onNavBack"
		showNavButton="true">
		<semantic:subHeader>
			<Bar id="headerBar">
				<contentMiddle>
					<SearchField
						id="searchField"
						showRefreshButton="{= !${device>/support/touch} }"
						tooltip="{i18n>masterSearchTooltip}"
						width="100%"
						search="onSearch">
					</SearchField>
				</contentMiddle>
			</Bar>
		</semantic:subHeader>

		<semantic:content>
			<PullToRefresh
				id="pullToRefresh"
				visible="{device>/support/touch}"
				refresh="onRefresh" />
			<!-- For client side filtering add this to the items attribute: parameters: {operationMode: 'Client'}}" -->
			
			<SelectList
				class="RenoPrice-Master-SelectMenu"
				itemPress="onMenuSelect"
				width="30%">
					<!-- <items>
						<Button
						class="RenoPrice-Master-Menu-FlatButton"
						text="TEST"
						press="onTestPress">
					</Button>
					</items> -->
					<core:Item key="TestKey" text="Test item" />
					


			</SelectList>

		</semantic:content>

	</semantic:MasterPage>

</mvc:View>
